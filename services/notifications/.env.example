# Copy this file to .env and adjust values for your machine
# RabbitMQ broker this service CONSUMES events from
RABBITMQ_URL="amqp://guest:guest@localhost:5672"

# HTTP port (only used to VIEW consumed notifications; the real
# input is RabbitMQ, not HTTP)
PORT=3003
