# Copy this file to .env and adjust values for your machine
# Where the internal services live (the gateway talks to them over HTTP)
PRODUCT_SERVICE_URL="http://localhost:3001"
ORDER_SERVICE_URL="http://localhost:3002"

# Port the gateway listens on (THE public door of the system)
PORT=3000
